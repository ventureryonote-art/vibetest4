# DESIGN.md — 「AIが作った感」を出さないための決めごと

Claude Code は、`CLAUDE.md` と一緒に**このファイルも毎回読みます。**
見た目に関する判断は、すべてここに従います。

> **なぜこれが要るのか**
> ランディングページ1,590件を機械判定した調査で、**54%が同じ見た目**をしていました。
> AIに任せると、誰が作っても同じ顔になります。**避けるのは才能ではなく、チェックリストの仕事です。**

---

## 0. 大前提 ── これは「アプリの画面」であって、宣伝ページではない

このテンプレートで作るのは、**その人が毎日開いて仕事を進める画面**です。
サービスを売り込むランディングページ（LP）ではありません。

| | ❌ 作らないもの（LP） | ✅ 作るもの（アプリ画面） |
|---|---|---|
| 冒頭 | 大きなキャッチコピー | **アプリ名＋今の状況**（未対応 3件 など） |
| 主役 | 説明文と画像 | **データの一覧と、それを操作するボタン** |
| ボタン | 「お問い合わせ」「無料で始める」 | **「追加」「完了」**など、その場で何かが起きるもの |
| 文章量 | 多い | **少ない。**説明より、動くことが優先 |
| 想定読者 | 見込み客 | **もう使うと決めている本人** |

### 全員が同じ画面にはならない

`app/page.tsx` の上の **TONE / DENSITY / LAYOUT / UNIT** を選ぶと、
同じコードのまま色・余白・並び方・言葉が変わります。選択肢は `app/globals.css` に用意済みです。
**自分で色やCSSを書き足さないでください。** 選び方と実例は `samples/画面の型.md`。

### 画面の基本の形（テンプレートに実装済み）

```
┌──────────┬────────────────────────────────┐
│ アプリ名   │ 一覧          説明        [新規登録] │ ← .topbar
│ ───────  ├────────────────────────────────┤
│ 一覧    2 │  [ 2 ]未対応 [ 1 ]3日以上 [ 3 ]全件   │ ← .stats
│ 新規登録  │  [検索                ][未対応|済|全] │ ← .filters
│ 設定     │  ┌──────────────────────────┐  │
│          │  │ 未対応（待たせている順）   2件 │  │ ← .list-head
│ ↑ .side  │  │ Aさん  週2希望  [4日][LINE]  │  │ ← .row
│          │  │        08/17 [編集][済][削除] │  │
│          │  └──────────────────────────┘  │
└──────────┴────────────────────────────────┘
```

**左メニューは必ず残してください。**これがあるかどうかで、業務アプリに見えるかが決まります。

### ⛔ 社内向けの文書を、画面に出さないこと

`docs/01_customer.md` や `docs/03_spec.md` の中身は、**作る側のためのメモ**です。
**画面に出してはいけません。**

特に次のものが画面に出ていたら、それは間違いです。

- **「やらないこと」の一覧** ← 社内の判断であって、使う人には関係ありません
- 🔮 の仮説、出口の3点、競合の分析
- 「こんなことが起きていませんか」のような**課題の説明セクション**
- 「まず1教室で試させてください」のような**営業文**

> これらは**要件定義書の中で使い切る**ものです。
> 画面には、**その結果として決まった「操作できるもの」だけ**を置きます。

---

## 1. 使う色・文字・余白（ここ以外を使わない）

すべて `app/globals.css` の先頭で定義済みです。**直値（`#1f5fd6` や `13px`）を書かないでください。**

| 用途 | 変数 |
|---|---|
| 背景 | `--bg` |
| カード面 | `--surface` / `--surface-2` |
| 罫線 | `--border` / `--border-strong` |
| 本文 | `--text` |
| 補助文 | `--text-sub` / `--text-weak` |
| アクセント（**1色だけ**） | `--accent` |
| 状態 | `--ok` / `--warn` / `--danger`（＋ `-weak` 版） |
| 余白 | `--s1` 〜 `--s12`（4の倍数のみ） |
| 文字サイズ | `--t-xs` 〜 `--t-2xl` |

### 使えるクラス（これを組み合わせて画面を作る）

| クラス | 用途 |
|---|---|
| `.app` / `.app-bar` / `.app-body` | 画面全体の器 |
| `.stats` / `.stat` | 上部の数字（未対応◯件 など） |
| `.toolbar` / `.field` / `.select` / `.btn` / `.btn-ghost` | 入力と操作 |
| `.list` / `.list-head` / `.row` / `.row-main` / `.row-title` / `.row-sub` / `.row-meta` | 一覧 |
| `.badge` / `.badge-ok` / `.badge-warn` / `.badge-danger` | ラベル |
| `.empty` | **0件のときの画面（必ず作る）** |

**見た目を大きく変えたいときは、個別のファイルではなく、この変数の値を変えます。**

---

## 2. やらないこと（AIっぽさの正体）

以下は、AI生成ページの「指紋」として実測で特定されたものです。**4つ以上当てはまると、ひと目で分かります。**

### 色

- ❌ **紫〜青のグラデーション**（通称 VibeCode Purple）。最も分かりやすい指紋です
- ❌ 背景に光る円（グロー、オーブ）
- ❌ すりガラス（グラスモーフィズム）を理由なく使う
- ❌ アクセント色を2色以上使う。**1色に絞ってください**
- ❌ 純粋な灰色（`#808080`）。**わずかにアクセント寄りに色を振った灰**を使う

### レイアウト

- ❌ **中央揃えの巨大ヒーロー**＋その上の丸いピル型バッジ
- ❌ **アイコン＋見出し＋説明の同じカードを3〜6枚**並べる
- ❌ 見出しの上に置く**角丸の四角いアイコン容器**
- ❌ **1-2-3の番号付きステップ図**（本当に順番が意味を持つとき以外）
- ❌ **「99% / 10x / 24/7」のような数字を横並び**にするバナー
- ❌ **カードの左端に色付きの縦線**（最も確実なAIの証拠と言われています）
- ❌ すべてを中央揃えにする

### 文字・装飾

- ❌ 見出しやリストに**絵文字**（✅🚀💡）を使う
- ❌ **ALL CAPS ＋ 字間を広げたラベル**の乱用
- ❌ 全部同じ角丸半径にする
- ❌ すべてのカードに同じ影と枠をつける

---

## 3. そのかわり、こうする

### ① 揃えすぎない

全部を同じ大きさ・同じ間隔にしない。**大事なものを大きく、そうでないものを小さく。**
カードを並べるなら、**1枚だけ大きくする**か、**そもそもカードにしない**。

### ② コントラストをはっきりつける

本文と背景の明暗差をしっかり取る。**薄いグレーの文字を薄い背景に置かない。**
「上品に見せる」ために薄くするのは、たいてい読みにくくなるだけです。

### ③ 余白を大胆に取る

要素と要素の間は、**思っているより広く。**
詰まっている画面は素人っぽく見えます。`--space-12` や `--space-16` を怖がらないこと。

### ④ アクセント色は「ここぞ」だけ

**1画面にアクセント色は1〜2箇所。**押してほしいボタン、いちばん見せたい数字。
それ以外は文字色と罫線だけで作れます。色数が減るほどプロっぽく見えます。

### ⑤ 押せるものは、押せるように見せる

ボタンには**ホバー時の変化**を必ずつける（`transition` を入れる）。
逆に、押せないものをボタンに見せない。

### ⑥ 空っぽの状態を設計する

データが0件のときの画面を必ず作る。**「まだありません」だけで終わらせず、次に何をすればいいかを書く。**
ここを作ってあるかどうかで、既製品っぽさが決まります。

---

## 4. 文言のAIっぽさ

見た目と同じくらい、言葉でバレます。

| ❌ 避ける | ✅ こう書く |
|---|---|
| 「シームレスに」「パワフルな」「革新的な」 | 具体的な動詞で書く |
| 「〜を実現します」 | 「〜できます」 |
| 「Get Started」「Learn More」 | **何が起きるかを書く**（「今日の分を見る」） |
| 「AI搭載」「AI-Powered」 | 使う人にとっての結果を書く |
| 誰にでも当てはまる説明 | **その人にしか刺さらない一文** |

ボタンの文言は、**押したあとに起きることをそのまま書く**のが一番外しません。

---

## 5. 出す前の確認（6つ）

`/check` を打つと、Claudeがこれを見ます。

- [ ] 直値の色・サイズを書いていない（すべて変数）
- [ ] アクセント色が1画面に1〜2箇所に収まっている
- [ ] 「やらないこと」に4つ以上当てはまっていない
- [ ] スマホ幅（375px）で横スクロールが出ない
- [ ] データが0件のときの画面がある
- [ ] ボタンの文言が「押したら何が起きるか」になっている

---

## 6. この決めごとを変えたいとき

**このファイルを直してください。**コードを直すより先です。

色やトーンを変えたい場合は、`app/globals.css` の変数を変えれば全体が変わります。
「もっと落ち着いた感じに」と頼むときも、**個別のページではなく変数を変えるよう指示**してください。
